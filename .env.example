# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# Django Settings
SECRET_KEY=your-django-secret-key-here
DEBUG=True

# Database Configuration (Neon.tech PostgreSQL)
# Get this from your Neon.tech dashboard after creating a database
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/soulence_db?sslmode=require

# Stream Chat API Credentials
STREAM_API_KEY=your-stream-api-key
STREAM_API_SECRET=your-stream-api-secret

# Allowed Hosts (comma-separated)
# Add your production domain when deploying
ALLOWED_HOSTS=127.0.0.1,localhost

# Optional: Email Configuration (for production)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
